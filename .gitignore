# Build output — regenerated by `npm run build`, never edit by hand.
_site/

# Dependencies — reinstalled with `npm install`.
node_modules/

# macOS noise.
.DS_Store

# Local Claude Code dev-server config (machine-specific paths).
.claude/
