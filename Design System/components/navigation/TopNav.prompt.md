Top navigation bar pinned to every page — Lantern torch mark + wordmark, primary menu, sign-in link, bronze "Learn Now" CTA.

```jsx
<TopNav activeLink="Techniques" onNavigate={setPage} />
```

Collapses to a hamburger + full-screen cream sheet below 768px (not modeled in this primitive — build per-kit if needed).
