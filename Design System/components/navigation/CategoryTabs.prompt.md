Sub-nav filter row used on tradition/technique pages — inactive tabs are transparent + muted text, active tab gets a cream-card background.

```jsx
<CategoryTabs tabs={['All','Longsword','Rapier','Sabre']} active="Longsword" onChange={setTab} />
```
