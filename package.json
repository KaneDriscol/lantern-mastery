{
  "name": "lantern-mastery",
  "version": "1.0.0",
  "description": "Kane's fencing (HEMA) working notebook — a static site built from Markdown with Eleventy.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "new": "node scripts/new-post.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it": "^14.1.0"
  }
}
